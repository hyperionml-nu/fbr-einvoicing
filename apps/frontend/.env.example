# Backend API base URL.
# Local:
REACT_APP_API_BASE_URL=http://localhost:3000

# Deployed with same-origin Cloudflare Worker/static assets:
# Leave unset so API requests go to /api on the same hostname.
#
# Deployed with separate backend:
# REACT_APP_API_BASE_URL=https://your-backend-url

